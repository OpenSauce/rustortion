[workspace]
members = [
    "rustortion-core",
    "rustortion-gui",
    "rustortion-plugin",
    "xtask",
]
resolver = "3"

[profile.dev]
opt-level = 1

[profile.dist]
inherits = "release"
codegen-units = 1
lto = true

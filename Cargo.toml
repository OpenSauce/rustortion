[workspace]
members = [
    "rustortion-core",
    "rustortion-standalone",
    "rustortion-plugin",
    "rustortion-ui",
    "xtask",
]
resolver = "3"

[profile.dev]
opt-level = 1

[profile.dist]
inherits = "release"
codegen-units = 1
lto = true
